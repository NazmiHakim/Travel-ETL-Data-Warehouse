# ============================================================
# TravelNusantara — Project Dependencies
# ============================================================
# Core data pipeline
pandas>=2.0.0
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
python-dotenv>=1.0.0
Faker>=24.0.0

# AI enrichment & agent
google-genai>=0.8.0
scikit-learn>=1.4.0
# Optional: install for dense semantic RAG embeddings (all-MiniLM-L6-v2)
# Falls back to TF-IDF automatically if not installed.
sentence-transformers>=3.0.0

# Web UI & visualization
streamlit>=1.35.0
plotly>=5.22.0

# API integration (optional — only required for extract_api.py)
amadeus>=8.1.0

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0
